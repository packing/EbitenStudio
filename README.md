# EbitenStudio

基于 Electron + Go (Ebiten) 的可视化 UI 编辑器

## 架构

```
┌─────────────────────────────────────┐
│        Electron 窗口                │
│  ┌─────────────┬─────────────────┐  │
│  │  工具栏     │   属性面板      │  │
│  ├─────────────┴─────────────────┤  │
│  │     Canvas 预览区             │  │
│  │  (Ebiten 独立窗口)            │  │
│  └───────────────────────────────┘  │
│           ↕ HTTP/WebSocket          │
└─────────────────────────────────────┘
                 ↕
┌─────────────────────────────────────┐
│         Go 后端服务                 │
│  ┌────────────┬──────────────────┐  │
│  │ Gin API    │  Ebiten Window   │  │
│  │  (REST)    │  (独立渲染)      │  │
│  └────────────┴──────────────────┘  │
│         共享状态 (Bridge)            │
└─────────────────────────────────────┘
```

## 技术栈

- **前端**: Electron + Vanilla JS + Chromium
- **后端**: Go + Gin + Ebiten v2
- **通信**: REST API + WebSocket

## 快速开始

### 1. 构建后端

```powershell
cd backend
go mod tidy
go build -o ../build/backend.exe
```

### 2. 安装前端依赖

```powershell
cd frontend
npm install
```

### 3. 运行应用

```powershell
# 方式1: 使用构建脚本
.\build.ps1

# 方式2: 手动启动
cd frontend
npm start
```

## 功能特性

- ✅ 实时预览 Ebiten Canvas (独立窗口)
- ✅ 拖拽创建 UI 组件
- ✅ 属性编辑器
- ✅ WebSocket 实时同步
- 🚧 组件层级管理
- 🚧 导出为 JSON 布局
- 🚧 代码生成

## API 端点

- `GET /api/widgets` - 获取所有组件
- `POST /api/widgets` - 创建新组件
- `PUT /api/widgets/:id` - 更新组件
- `DELETE /api/widgets/:id` - 删除组件
- `WS /api/events` - WebSocket 事件流

## 项目结构

```
EbitenStudio/
├── backend/          # Go 后端服务
│   ├── api/          # HTTP API
│   ├── bridge/       # 状态桥接
│   ├── canvas/       # Ebiten 渲染
│   └── pkg/          # 核心包
└── frontend/         # Electron 前端
    └── src/          # 前端代码
```

## 开发说明

### 运行要求

- Go 1.21+
- Node.js 18+
- Windows/Linux/macOS

### 首次运行

1. 等待依赖下载完成
2. Electron 窗口会显示加载页面,2秒后连接后端
3. 会同时打开两个窗口:
   - Electron 主窗口 (控制界面)
   - Ebiten Canvas 窗口 (实时预览)

### 架构特点

- **双窗口设计**: 编辑器和预览分离,互不干扰
- **状态同步**: 通过 WebSocket 实时同步所有变更
- **所见即所得**: Canvas 使用 Ebiten 渲染,与运行时一致
- **跨平台**: Electron 提供一致的 UI 体验

## License

MIT
