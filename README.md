# EbitenStudio

基于 Electron + Go (Ebiten) 的可视化 UI 编辑器

## 架构

```
┌─────────────────────────────────────┐
│        Electron 窗口                │
│  ┌─────────────┬─────────────────┐  │
│  │  工具栏     │   属性面板      │  │
│  ├─────────────┴─────────────────┤  │
│  │   HTML Canvas 编辑区          │  │
│  │   (实时编辑和预览)            │  │
│  └───────────────────────────────┘  │
│                                     │
│  预览按钮 → 启动 Go Viewer         │
└─────────────────────────────────────┘
                 ↓
┌─────────────────────────────────────┐
│      Go Viewer (独立进程)          │
│    Ebiten 渲染引擎预览UI           │
│    加载 .ui + .pak 资源包          │
└─────────────────────────────────────┘
```

## 技术栈

- **编辑器**: Electron + Vanilla JS + HTML Canvas
- **预览器**: Go + Ebiten v2
- **UI库**: Go + Ebiten (可独立使用)
- **通信**: IPC (Electron进程间通信)

## 快速开始

### 1. 安装前端依赖

```powershell
cd frontend
npm install
```

### 2. 运行编辑器

```powershell
cd frontend
npm start
```

### 3. 编译预览器（可选）

```powershell
cd ui\examples\viewer
go build -o viewer.exe
```

## 功能特性

- ✅ 拖拽创建 UI 组件
- ✅ 属性编辑器 (Padding/Margin/样式)
- ✅ HTML Canvas 实时编辑
- ✅ 一键预览 (启动 Go Viewer)
- ✅ 导出为 .ui + .pak 资源包
- ✅ 项目保存/加载 (.ebiten格式)
- ✅ 资源管理 (图片/字体)
- 🚧 组件层级管理
- 🚧 热重载
- 🚧 代码生成

## 项目结构

```
EbitenStudio/
├── frontend/         # Electron 编辑器
│   ├── src/
│   │   ├── js/       # 编辑器逻辑
│   │   │   ├── widgets/  # Widget类系统
│   │   │   ├── app.js
│   │   │   ├── canvas-renderer.js
│   │   │   ├── preview.js
│   │   │   └── resource-manager.js
│   │   └── styles/
│   ├── index.html
│   └── package.json
└── ui/               # Go UI 运行时库
    ├── *.go          # Widget实现
    ├── loader.go     # JSON/Pak加载器
    ├── renderer.go   # Ebiten渲染器
    └── examples/
        └── viewer/   # 预览器应用
            └── main.go
```

## 开发说明

### 运行要求

- Node.js 18+
- Go 1.21+ (用于预览和打包)
- Windows/Linux/macOS

### 工作流程

1. **编辑UI**: 
   - 在 Electron 编辑器中设计界面
   - 使用 HTML Canvas 实时预览

2. **预览效果**: 
   - 点击 "▶️ 预览" 按钮
   - 自动启动 Go Viewer 查看实际渲染效果

3. **导出发布**: 
   - 点击 "💾 导出" 按钮
   - 生成 .ui + .pak 资源包

4. **游戏集成**: 
   - 在 Go 游戏中引用 ui 库
   - 加载导出的 .ui 文件

### 架构特点

- **分离式设计**: 编辑器和预览器独立运行
- **所见即所得**: Viewer 使用 Ebiten 渲染，与游戏运行时一致
- **资源打包**: .pak 格式整合所有资源，便于发布
- **跨平台**: Electron 和 Ebiten 均支持多平台

## License

MIT
